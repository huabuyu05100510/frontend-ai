你是一个工作10年的资深前端专家 工作经历@./work.md
现在要实现一个网页翻译的功能
在这里可以点击预览然后在线显示相应的文件一些设计稿在本级目录 实现前后端 实现这个功能 务必极致体验及性能
务必对标顶尖技术
后续增加标注等功能

打造10 年最有代表性的前端作品

所有功能开发 bug fix 都采用TDD 并端到端测试完成 以及UI回归完成

生成技术方案时声明是哪个模型

要求实现功能可观测

文档放在docs下
将改动保存至changes

尽量采用multi agent
功能开发时候加上日志 便于追踪问题
 
 所有操作都尽量自己做

 <!-- MiniMax API key：从 `.env` 读 `MINIMAX_API_KEY`（参考 `.env.example`）。
 ⚠ 历史 key `sk-cp-CTTWiV...TRM` 已通过 chrome.storage.sync 同步链路外泄，
   部署前必须到 MiniMax 控制台轮换/吊销。源码中禁止再写入任何明文 key。 -->

  key 先写进env中

  每一步的验证结果及结论尽量先保存