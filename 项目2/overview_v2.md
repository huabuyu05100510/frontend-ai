---

### **个人亮点**
- **10年+前端开发经验**，具备**从 0 到 1 独立负责复杂 ToB SaaS 平台架构设计与商业化落地**的能力。
- **大文件与富媒体处理专家**：主导实现浏览器端 23 种格式文档、1GB+ 音视频的预览/编辑/标注/导出全闭环，并深入音视频编辑领域——基于 WebAudio API 构建在线配音时间轴编辑器，覆盖虚拟渲染、Web Worker、Range Request、WebAudio 完整技术体系，无需客户端插件。
- **协同系统设计者**：具备实时多人协同编辑系统的完整落地经验，基于 CRDT（Yjs）解决多人编辑冲突，Awareness 协议实现光标感知，支持离线恢复与百人并发。
- **AI 能力产品化专家**：擅长将语音转写（实时 PCM 流 + VAD 静音过滤）、OCR、多模态翻译、TTS 合成等 AI 接口封装为可商业化的前端产品；主导的平台合计年 ARR 近千万，3 个平台实现盈利。
- **工程化与团队建设**：主导过公司级组件库与跨平台公共能力层建设，复用率 70%+；  fgmn 带领 4 人团队在多平台并行场景下持续高质量交付。

---

### **技术能力**
- **核心框架与原理**：精通 **React(18)** / **Vue(3)**，深入理解响应式原理、Diff 算法、Fiber 调度机制，具备大型项目性能调优实战经验。
- **文档与富媒体处理**：精通 **Canvas / SVG**，熟练使用 **PDF.js、ProseMirror、Monaco Editor、pdf-lib**；精通 **WebAudio API / AudioContext / AudioWorklet**，具备音频时间轴编辑、PCM 流处理、多轨混音与 WAV 导出的完整工程经验；具备复杂文档渲染、编辑器内核定制、大文件分片上传与流式播放的完整工程经验。
- **实时通信与协同**：精通 **WebSocket / SSE**，落地过基于 **CRDT（Yjs）** 的多人协同编辑系统；熟练使用 **Web Worker、SharedArrayBuffer** 处理计算密集型任务；具备 **WebAudio API + VAD** 实时语音采集与流式 ASR 集成经验。
- **前端工程化与构建**：精通 **Vite / Webpack**，主导过大型项目构建性能优化（冷启动、打包体积、缓存策略）；熟练应用 **Monorepo（pnpm）**，主导过跨项目组件库建设。
- **架构设计与状态管理**：熟练应用 **Redux / Zustand / Pinia**，具备微前端、插件化平台（iframe 沙箱 + postMessage 协议）等架构落地经验；具备运行时配置注入（`window.__CONFIG__`）支持私有化多环境免重新构建的实战经验。
- **TypeScript 与大前端**：精通 **TypeScript**，具备大型项目应用经验；熟练使用 **Node.js（NestJS）** 处理 BFF 层需求。

---

### **项目经历**

#### **科大讯飞 ToB SaaS 矩阵**（20XX.XX - 20XX.XX）
**技术栈**：React18, TypeScript, ProseMirror, Yjs, WebSocket/SSE, Canvas, Web Worker, pdf-lib, Monaco Editor, WebAudio API

- **项目背景 (Situation)**：公司 AI 能力（语音转写、OCR 识别、多模态翻译、TTS 合成）缺乏面向企业客户的产品载体；传统桌面客户端部署成本高、迭代慢，ToB 场景下大体量文档处理（23 种格式、1GB+ 媒体）、多人协作审校、在线配音、合规签署等核心诉求在业界缺乏成熟的纯前端解决方案，制约了 AI 能力的商业化速度。

- **我的职责 (Action)**：

  1. **担任核心开发与架构设计**，主导 6 个 SaaS 平台（智能翻译、OCR 规则训练、质检、电子签、AILab 等）从 0 到 1 搭建，覆盖架构选型、技术攻关、私有化部署交付全流程，高峰期带领 4 人团队并行迭代。

  2. **设计并实现浏览器端全模态文档处理引擎**，支持 23 种文档格式（PDF/DOCX/PPT/XLS/SRT 等）、8 种音频、9 种视频格式，文件支持 1GB+，实现预览、编辑、标注、导出完整闭环；核心方案：服务端统一转换管道 + PDF.js + Canvas 分页渲染 + 虚拟页面池（仅维护可视区 ±2 页实例）+ Web Worker 异步解析 + HTTP Range Request 按需加载，首页可见时间从 8s 降至 2.4s 以内（P75），内存峰值降低 60%+。

  3. **攻克多人实时协同编辑难题**，引入 CRDT（Yjs）无冲突合并算法，彻底解决多人并发编辑冲突；Awareness 协议实现用户光标/选区实时感知；设计段落级编辑锁防止译员重复翻译；增量 Op 传输（单次 < 1KB）支持百人并发，IndexedDB 持久化保障离线操作断线后自动恢复。

  4. **构建所见即所得文档编辑能力**，基于 ProseMirror 打造富文本编辑内核，维护文档 AST 与渲染层双向同步；DOCX/XLSX/PPT 采用 JSZip + xml2js 结构化解析与二进制序列化导出，格式还原度 95%+；设计段落级双栏译文对照编辑器（Myers Diff 字符级高亮 + react-window 虚拟列表），万级段落场景下无卡顿。

  5. **负责文本校对与合规检查平台建设**，设计三层混合规则引擎（关键词黑名单 → 正则匹配 → AI 语义检测），结果按高危/中危/低危分级并精确定位到段落字符区间；实时校对采用 300ms 防抖 + SSE 流式返回 + 字符级 Diff 高亮，支持逐条 Accept/Reject；审计日志完整留存，支持合规报告一键导出。

  6. **交付电子签全链路**，Canvas 三阶贝塞尔曲线实现手写签名，笔迹平滑无锯齿，支持触控；pdf-lib 将签章图片写入 PDF 指定坐标，SHA-256 哈希锁定签后文档完整性；有限状态机管理多方签署流程（顺序签/并行签，状态：待发起 → 签署中 → 完成/拒签/过期），WebSocket 实时推送进度，动态水印 + 防截图保障合同安全。

  7. **建设 AILab 能力集市平台**，Monaco Editor + 虚拟目录树实现多种代码仓库在线预览，体验对齐 GitHub Web IDE；设计 iframe 沙箱 + postMessage 标准通信协议，支持 OCR、语音转写等 AI Demo 以 JSON 配置零代码接入，新能力上线无需改动平台代码，实现平台与 Demo 完全解耦。

  8. **实现网页翻译与实时语音转写两大 AI 交互能力**：
     - **网页翻译**：无需安装插件，通过脚本注入目标页面，MutationObserver 监听 SPA 动态节点变化，TreeWalker 批量遍历文本节点并替换译文，完整保留原始 DOM 结构与 CSS 样式；针对跨域场景设计 postMessage 桥接通信，对页面路由切换实现自动重注入，支持主流浏览器。
     - **实时语音转写**：WebAudio API + AudioWorklet 采集麦克风原始 PCM 流，结合能量阈值 + 过零率 VAD 过滤静音帧（有效降低上行带宽约 50%），有效帧经 WebSocket 实时推送 ASR 服务；SSE 流式接收逐词识别结果，逐字追加渲染实现"打字机效果"，端到端转写延迟控制在 800ms 以内。

  9. **构建在线配音制作能力**，基于 WebAudio API 设计音频时间轴编辑器，支持多段 TTS 合成片段的拖拽排列与波形可视化预览；AudioContext 调度多轨音频（配音轨 + 背景音乐轨），各轨音量独立增益控制；导出时在 Web Worker 线程完成 PCM 帧拼接与 WAV 封装，主线程零阻塞；字幕轨与音频轨通过时间码绑定，逐句对齐校验，支持语速/音调参数调节，一键导出带字幕的混合音频文件。

  10. **搭建 OCR 规则训练平台的图像标注工具**，Canvas 实现矩形框选、多边形标注及自由缩放拖拽；坐标系变换矩阵确保标注框在任意缩放比例下精准映射回原图像素坐标；基于 Command 模式实现无限步撤销/重做栈；标注数据与训练任务绑定，支持批量审核与置信度可视化，构成从数据标注到模型迭代的完整 MLOps 前端闭环。

  11. **沉淀跨平台公共组件库与工程规范**，抽象文件上传器、标注画板、AI 流式输出面板、媒体播放器、音频时间轴等核心组件，复用率 70%+；构建运行时配置注入方案（`window.__CONFIG__`），免重新构建支持多套私有化环境差异配置；Nginx 反代 + CSP 白名单配置脚本化，私有化部署交付周期从 5-7 天压缩至 1-2 天；制定 Code Review 标准、分支策略与性能预算机制，保障多团队高质量并行交付。

- **项目成果 (Result)**：
  - **业务赋能**：3 个平台实现商业化盈利，合计年 ARR **近千万**；纯浏览器方案消除客户本地安装门槛，合同签署周期从**天级缩短至分钟级**，私有化部署交付周期从 **5-7 天压缩至 1-2 天**，直接支撑大客户签约与续约。
  - **效率提升**：多译员协同将翻译团队串行工作流升级为并行作业，人均日产能显著提升；公共组件库使新平台启动成本降低 **70%+**；AI Demo 接入从「开发上线」变为「配置上线」，产品迭代速度显著提升。
  - **性能指标**：大文档首页可见时间从 8s 降至 2.4s 以内（P75），内存峰值降低 60%+；实时语音转写端到端延迟 < 800ms；VAD 静音过滤使上行带宽降低约 50%。
  - **技术沉淀**：形成覆盖「大文件处理 / 多人协同 / AI 流式渲染 / 文档引擎 / 音视频编辑 / 插件化平台 / 图像标注工具」的前端技术体系，成为部门前端架构标准底座，可直接复用至后续产品线。

---

## 面试深挖速查

| 方向 | 高频问题 | 核心答案 |
|------|---------|---------|
| 大文件 | 1GB文件上传怎么保证成功率？ | 5MB分片 + MD5秒传 + 断点续传，失败只重传出错的片段 |
| 渲染 | 23种格式怎么不各写一套？ | 服务端统一转PDF管道，前端一套渲染逻辑，格式差异在服务端消化 |
| 内存 | 大文档怎么不崩溃？ | 虚拟页面池LRU淘汰 + revokeObjectURL + Worker独立堆内存 |
| 编辑 | DOCX编辑后格式为什么不乱？ | JSZip解压→精准修改目标XML节点→重新打包，不碰其他节点 |
| 协同 | 为什么选CRDT不用OT？ | OT需要中央服务器做变换复杂度高，CRDT操作可交换天然支持离线和P2P |
| 协同 | 百人并发怎么不卡？ | 只传增量Op（<1KB）+ 服务端广播，不同步全文档状态 |
| 协同 | 段落锁怎么实现的？ | 编辑时广播锁定Op，其他客户端收到后UI置灰该段落，释放时广播解锁 |
| 签名 | 手写签名为什么不锯齿？ | 采集压力点序列，三阶贝塞尔曲线拟合，平滑连续而非直线连点 |
| 合规 | 规则引擎三层怎么合并结果？ | 以字符区间为key做并集，相同区间取最高风险等级，去重后统一渲染 |
| AILab | 新Demo怎么零改动上线？ | JSON配置驱动动态注册，iframe沙箱隔离，postMessage约定消息格式 |
| 架构 | 怎么管理6个平台不重复造轮子？ | Monorepo统一仓库 + 公共组件库 + 通用Hooks，各平台按需引入 |
| 网页翻译 | 不装插件怎么实现网页翻译？ | 脚本注入页面，TreeWalker遍历文本节点替换，MutationObserver监听SPA动态内容，路由切换自动重注入 |
| 语音转写 | 实时转写延迟怎么控制在800ms？ | AudioWorklet采集PCM帧 + VAD过滤静音 + WebSocket低延迟传输，SSE逐词流式返回渲染 |
| 语音转写 | VAD怎么实现的？ | 能量阈值 + 过零率双重判断，有效帧才上传，降低上行带宽约50% |
| 配音制作 | 多轨音频时间轴怎么做的？ | AudioContext统一时间基准，各轨独立GainNode控制音量，导出在Worker里拼接PCM帧封WAV |
| 配音制作 | 字幕和音频怎么对齐？ | 时间码绑定，每段TTS合成后拿到精确时长，字幕轨按时间码偏移渲染，逐句校验误差 |
| 标注工具 | 图片标注坐标怎么对齐原图像素？ | Canvas缩放比例矩阵，鼠标事件坐标除以缩放比 + 减去偏移量，逆变换还原到原图坐标系 |
| 标注工具 | 撤销重做怎么实现的？ | Command模式，每个操作封装成execute/undo函数，维护操作栈push/pop |
| 私有化 | 前端怎么支持多套私有化环境免重新构建？ | 构建时不hardcode环境变量，运行时读window.__CONFIG__，Nginx在HTML里注入不同env对象 |
| 私有化 | 部署周期怎么从5-7天压到1-2天？ | Nginx配置模板化 + 脚本一键生成CSP白名单 + docker-compose统一编排，消除人工逐项配置 |
